import { get } from '@vercel/blob';
import { getPlatformEnv } from '../../../../lib/platform-env';
export async function GET(_:Request,{params}:{params:Promise<{key:string}>}){const {key}=await params;if(process.env.BLOB_READ_WRITE_TOKEN){const object=await get(key,{access:'private'});if(!object||object.statusCode!==200)return new Response('Not found',{status:404});return new Response(object.stream,{headers:{'content-type':object.blob.contentType,'cache-control':'public, max-age=31536000, immutable'}});}const env=await getPlatformEnv();if(!env)return new Response('Not found',{status:404});const object=await env.FILES.get(key);if(!object)return new Response('Not found',{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set('etag',object.httpEtag);headers.set('cache-control','public, max-age=31536000, immutable');return new Response(object.body,{headers});}
